import React from "react";
import Container from "src/components/containers/Container";

const TodosPage: React.FC = (): React.ReactElement => {
  return <Container>Todos Page</Container>;
};

export default TodosPage;
